# Build artifacts & dependencies (rebuilt inside the image)
node_modules
dist
*.tsbuildinfo

# VCS & CI
.git
.gitignore
.gitattributes
.github

# Secrets — never bake them into a layer, they are injected at runtime
.env
.env.*
!.env.example

# Tooling / docs, irrelevant to the runtime
README.md
CLAUDE.md
apache.conf
eslint.config.mts
Dockerfile.dev
Dockerfile.prod
docker-compose.yml
.dockerignore
.vscode
.idea
*.log
npm-debug.log*
coverage
