# bravo-vous-etes-sur-internet.fr

Thierry Lhermitte.

## 🚀 - Quick start

```sh
git clone git@github.com:LalbaAnthony/bravo-vous-etes-sur-internet.git

npm install

cp .env.example .env

docker compose --profile development down ; docker compose --profile development up --build -d
```